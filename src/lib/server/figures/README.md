# `server/figures/`

Figure interpreter (Nice tier). Extracts figure regions from PDFs and asks the AI provider to describe them, so figures show up usefully in summaries and search.

Currently `.gitkeep`-only and **deferred** — only built if Nice-tier scope holds. Implementation tracked under **Issue 06**.

